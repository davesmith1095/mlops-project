name: wine_quality_project

entry_points:
  main:
    parameters:
      n_estimators: {type: int, default: 100}
      max_depth: {type: int, default: 10}
    command: "python src/train.py --n_estimators {n_estimators} --max_depth {max_depth}"